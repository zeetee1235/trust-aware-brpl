\documentclass[10pt,conference]{IEEEtran}
% Language policy applied:
% - Main text: Korean
% - Abstract/Keywords: English
% - Figure captions: English (can be switched to Korean if needed)

\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{kotex}
\usepackage{amsmath,amssymb}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{multirow}
\usepackage{siunitx}
\usepackage{xcolor}
\usepackage{url}
\usepackage[hidelinks]{hyperref}
\usepackage[numbers]{natbib}

\graphicspath{{figures/}}

\newcommand{\todo}[1]{\textcolor{red}{[TODO: #1]}}
\newcommand{\paperfig}[2]{%
  \IfFileExists{#1.pdf}{\includegraphics[width=#2\linewidth]{#1.pdf}}{%
    \IfFileExists{#1.png}{\includegraphics[width=#2\linewidth]{#1.png}}{%
      \fbox{\parbox[c][0.18\textheight][c]{0.9\linewidth}{\centering Missing figure file: #1.(pdf/png)\\Convert SVG in docs/paper/figures if needed.}}%
    }%
  }%
}

\title{TA-BRPL: Trust-Aware Backpressure RPL Against Combined Sinkhole and Grayhole Attacks}

\author{\IEEEauthorblockN{TODO}
\IEEEauthorblockA{TODO Institution\\
Email: TODO}}

\begin{document}
\maketitle

\begin{abstract}
Low-Power and Lossy Networks (LLNs) using RPL/BRPL are vulnerable to insider routing attacks that manipulate control-plane advertisements and data-plane forwarding. This paper presents TA-BRPL, a trust-aware extension of BRPL that integrates trust penalties into parent selection and forwarding decisions, with a blacklist policy for persistent offenders. We model sinkhole behavior through rank/ETX manipulation and grayhole behavior through selective packet dropping, and evaluate resilience across topology classes (Cluster, Grid, Ring) and attack rates from 0\% to 100\%. Our implementation is fully integrated into Contiki-NG/Cooja with a reproducible batch pipeline. \todo{Insert final quantitative summary: mean PDR gain and high-attack regime gain.}
\end{abstract}

\begin{IEEEkeywords}
RPL, BRPL, trust-aware routing, sinkhole, grayhole, IoT security, LLN.
\end{IEEEkeywords}

\section{서론}
저전력·손실 네트워크(LLN)에서 라우팅 프로토콜은 제한된 자원 환경에서도 안정적인 전송 성능을 유지해야 한다. BRPL은 RPL 구조에 backpressure 개념을 결합하여 혼잡 환경에서 처리량과 지연 특성을 개선하지만, 악성 노드에 대한 신뢰도 기반 억제 메커니즘은 기본적으로 포함하지 않는다. 이로 인해 공격 노드가 부모 후보로 과도하게 선택되는 경우 전달 성능이 급격히 저하될 수 있다.

본 논문은 sinkhole과 grayhole이 동시에 존재하는 혼합 공격 환경에서 TA-BRPL이 PDR 저하를 얼마나 완화할 수 있는지 분석하는 것을 목표로 한다. 핵심 질문은 공격 강도가 증가할 때 trust 기반 penalty가 붕괴 시점을 지연시키고, 동시에 정상 조건에서 과도한 성능 손실을 만들지 않는지이다.

\subsection{기여점}
\begin{itemize}
  \item BRPL metric에 trust penalty와 blacklist hysteresis를 결합한 TA-BRPL 설계.
  \item rank/ETX 조작(sinkhole)과 선택적 드롭(grayhole)을 결합한 공격 모델 구현.
  \item Cluster/Grid/Ring 토폴로지와 공격률 스윕을 포함한 재현 가능한 실험 파이프라인.
  \item 공정성(공격 off), 강건성(공격 on), trust 효과, attacker exposure를 분리한 결과 구성 체계.
\end{itemize}

\section{시스템 개요}
Figure~\ref{fig:arch}는 TA-BRPL 동작 흐름을 보여준다. 패킷 전달 관찰값을 기반으로 trust를 계산하고, 이를 BRPL score에 penalty로 반영한 뒤, blacklist 정책으로 후보를 제한하여 최종 부모 선택에 반영한다.

\begin{figure}[t]
  \centering
  \paperfig{fig1_ta_brpl_architecture}{0.98}
  \caption{TA-BRPL architecture: trust observation, trust calculation, trust-penalized BRPL metric, blacklist policy, and parent selection.}
  \label{fig:arch}
\end{figure}

\section{공격 모델}
공격자는 주요 포워딩 경로 인근에 배치되며 다음 두 동작을 결합한다.
\begin{itemize}
  \item \textbf{Sinkhole 조작}: DIO에서 낮은 rank 및 유리한 ETX를 광고하여 트래픽을 유인.
  \item \textbf{Grayhole 동작}: 자신을 경유하는 패킷을 확률적으로 드롭.
\end{itemize}

\begin{figure}[t]
  \centering
  \paperfig{fig2_attack_model}{0.98}
  \caption{Combined attack model: control-plane attraction (sinkhole) and data-plane disruption (grayhole).}
  \label{fig:attack}
\end{figure}

\section{TA-BRPL 설계}
\subsection{Trust 입력}
TA-BRPL은 데이터 평면의 포워딩 성공/실패 관측과 제어 평면의 이상 징후를 결합해 노드별 trust를 산출한다. trust는 $[0,1]$ 범위로 정규화하며, 순간 변동의 영향을 줄이기 위해 시간 축 smoothing을 적용한다.

\subsection{BRPL metric 내 trust penalty}
노드 $i$가 후보 부모 $j$를 평가할 때 trust를 반영한 점수는 다음과 같이 정의한다.
\begin{equation}
  S_j = S^{\text{BRPL}}_j \cdot f(T_j, \lambda, \gamma),
\end{equation}
여기서 $T_j$는 후보 신뢰도, $\lambda$는 penalty 강도, $\gamma$는 비선형 민감도를 의미한다. 신뢰도가 낮은 후보는 링크/경로 메트릭이 좋아도 최종 선택 우선순위가 감소한다.

\subsection{Blacklist 정책}
신뢰도 변동에 의한 플래핑을 줄이기 위해 히스테리시스 임계값을 적용한다.
\begin{itemize}
  \item $T_j < T_{\text{blacklist}}$ 이면 blacklist 추가
  \item $T_j \ge T_{\text{clear}}$ 일 때만 blacklist 해제
\end{itemize}
단, 일반적으로 $T_{\text{clear}} > T_{\text{blacklist}}$ 로 설정한다.

\section{구현}
TA-BRPL은 Contiki-NG RPL-Classic 경로에 통합되며, mote 코드에서 trust/blacklist 적용이 수행된다. 실험 자동화는 동적 큐 기반 병렬 실행 스크립트로 구성되며, 로그 파서 및 그림 생성 스크립트로 결과를 재현 가능하게 관리한다.

\section{실험 설정}
\subsection{토폴로지}
실험은 100\,$\times$\,100 필드에서 Cluster, Grid, Ring 세 계열을 사용하며, 각 계열은 S/M/L 고정 좌표 세트를 사용한다.

\begin{figure}[t]
  \centering
  \paperfig{fig3_topologies_medium}{0.98}
  \caption{Representative medium-scale topologies used in experiments.}
  \label{fig:topo}
\end{figure}

\subsection{시나리오}
핵심 시나리오는 다음 여섯 가지다.
\begin{enumerate}
  \item RPL normal
  \item BRPL normal
  \item TA-BRPL normal
  \item RPL attack
  \item BRPL attack
  \item TA-BRPL attack
\end{enumerate}

공격 시나리오는 혼합공격(combined)을 사용하며, 정상 시나리오는 공격 효과가 비활성화된 설정으로 분리한다.

\subsection{스윕 및 평가지표}
\begin{itemize}
  \item 공격률: 0\%--100\% (5\% 간격)
  \item 주요 지표: PDR, attacker exposure, trust/blacklist 동작
  \item 보조 지표: parent switching(라우팅 안정성)
\end{itemize}

\section{결과 구성 (데이터 확정 전 템플릿)}
본 절은 실험 완료 직후 수치만 삽입하면 최종 원고로 전환할 수 있도록 템플릿 형태로 작성한다.

\subsection{공격 없음 조건의 공정성}
Figure 4에서 RPL/BRPL/TA-BRPL의 무공격 PDR을 비교한다.
\begin{itemize}
  \item 기대 결과: 정상 조건에서는 성능 차이가 크지 않아야 함.
  \item \todo{평균/분산/신뢰구간 수치 삽입}
\end{itemize}

\subsection{공격률 증가에 따른 강건성}
Figure 5--7에서 토폴로지별 PDR vs attack rate를 제시한다.
\begin{itemize}
  \item \todo{50/70/90\% 구간 이득 수치 삽입}
  \item \todo{고공격률 구간에서 이득 축소 원인 분석 문장 삽입}
\end{itemize}

\subsection{Trust 효과와 공격자 노출}
Figure 8에서 BRPL 대비 TA-BRPL의 attacker parent 사용 비율 감소를 제시한다.
\begin{itemize}
  \item \todo{평균 노출 감소율 수치 삽입}
  \item \todo{노출 감소와 PDR 향상 상관 해석 삽입}
\end{itemize}

\subsection{선택 결과: 내부 동작}
보조 그림으로 trust 시계열과 blacklist 트리거 시점을 제시할 수 있다.
\begin{itemize}
  \item \todo{대표 run 1개 기반 주석형 해석 추가}
\end{itemize}

\section{타당성 위협}
\begin{itemize}
  \item \textbf{시뮬레이션 한계}: 실제 무선 환경의 모든 변수를 반영하지 못할 수 있음.
  \item \textbf{공격자 수}: 단일 공격자 가정으로 다중 협력 공격은 미포함.
  \item \textbf{파라미터 민감도}: $\lambda$, $\gamma$, 임계값 설정에 따라 결과가 변동 가능.
  \item \textbf{토폴로지 의존성}: 경로 다양성/병목 구조에 따라 이득 폭이 다를 수 있음.
\end{itemize}

\section{결론}
본 연구는 BRPL에 trust penalty와 blacklist hysteresis를 결합한 TA-BRPL을 제안하고, 혼합공격 환경에서의 강건성 평가 체계를 제시했다. 구현과 실험 파이프라인은 재현 가능하도록 구성되었으며, \todo{최종 수치 기반 결론 문장 삽입}을 통해 논문 최종판을 완성할 수 있다.

\section*{재현성 노트}
본 초안의 그림은 `docs/paper/scripts` 하위 스크립트로 생성되며, 실험 실행은 병렬 동적 큐 스크립트(`scripts/`)를 통해 재현 가능하다.

\bibliographystyle{IEEEtran}
\bibliography{references}

\end{document}
